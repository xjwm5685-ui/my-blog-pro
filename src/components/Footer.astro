---
import { SITE_CONFIG } from '@/config';
import Equalizer from './Equalizer.astro';
const year = new Date().getFullYear();
---

<footer class="ftr">
  <!-- Giant Background Seal SVG -->
  <svg class="ftr__seal" viewBox="0 0 400 400" fill="none" stroke="currentColor" aria-hidden="true">
    <circle cx="200" cy="200" r="190" stroke-width="1" stroke-dasharray="2 6" opacity="0.15"/>
    <circle cx="200" cy="200" r="140" stroke-width="0.5" opacity="0.2"/>
    <circle cx="200" cy="200" r="90" stroke-width="1" stroke-dasharray="20 10 5 10" opacity="0.3"/>
    <path d="M200 10 L200 390 M10 200 L390 200" stroke-width="0.5" opacity="0.1"/>
    <path d="M60 60 L340 340 M60 340 L340 60" stroke-width="0.5" opacity="0.1"/>
    <circle cx="200" cy="200" r="40" stroke-width="1.5" opacity="0.4"/>
    <rect x="180" y="180" width="40" height="40" stroke-width="1" opacity="0.5" transform="rotate(45 200 200)"/>
    <text x="200" y="32" text-anchor="middle" font-family="var(--font-mono)" font-size="12" letter-spacing="4" opacity="0.2" fill="currentColor" stroke="none">SYSTEM CORE</text>
    <text x="200" y="376" text-anchor="middle" font-family="var(--font-mono)" font-size="12" letter-spacing="4" opacity="0.2" fill="currentColor" stroke="none">ONLINE</text>
  </svg>

  <div class="ftr__top">
    <div class="ftr__col">
      <span class="bracket-label">FOLLOW</span>
      <ul>
        <li><a href={SITE_CONFIG.github} target="_blank" rel="noopener">GitHub</a></li>
        <li><a href={SITE_CONFIG.bilibili} target="_blank" rel="noopener">Bilibili</a></li>
        <li><a href={`mailto:${SITE_CONFIG.email}`}>Email</a></li>
        <li><a href="/rss.xml">RSS</a></li>
      </ul>
    </div>

    <div class="ftr__col">
      <span class="bracket-label">CATEGORIES</span>
      <ul>
        <li><a href="/category/tech">技术</a></li>
        <li><a href="/category/notes">笔记</a></li>
        <li><a href="/category/travel">游记</a></li>
        <li><a href="/tags">所有标签</a></li>
      </ul>
    </div>

    <div class="ftr__col">
      <span class="bracket-label">EXPLORE</span>
      <ul>
        <li><a href="/blog">归档</a></li>
        <li><a href="/projects">成品项目</a></li>
        <li><a href="/videos">视频教程</a></li>
        <li><a href="/about">关于</a></li>
      </ul>
    </div>

    <div class="ftr__col">
      <span class="bracket-label">CONTACT</span>
      <ul>
        <li><span class="muted">{SITE_CONFIG.email}</span></li>
        <li><span class="muted">微信公众号</span></li>
        <li><span class="muted">{SITE_CONFIG.wechat}</span></li>
      </ul>
    </div>

    <div class="ftr__col ftr__brand">
      <a href="/" class="ftr__wordmark">{SITE_CONFIG.shortTitle}</a>
      <p class="ftr__tag">{SITE_CONFIG.subtitle}</p>
      <div class="ftr__live">
        <Equalizer />
        <span class="ftr__live-text">
          <span class="ftr__live-label">BROADCASTING</span>
          <span class="ftr__live-since">EST · 2024</span>
        </span>
      </div>
    </div>
  </div>

  <div class="ftr__bottom">
    <span class="mono">© {year} {SITE_CONFIG.author} · ALL RIGHTS RESERVED</span>
    <span class="mono ftr__site">BUILT WITH ASTRO · DEPLOYED ON VERCEL</span>
  </div>
</footer>

<style>
  .ftr {
    position: relative;
    margin-top: var(--s-20);
    padding: var(--s-10) var(--shell-px) var(--s-6);
    border-top: 1px solid var(--border);
    background: linear-gradient(180deg, transparent, rgba(255,255,255,0.015));
    overflow: hidden;
  }

  .ftr__seal {
    position: absolute;
    top: 50%;
    right: -100px;
    transform: translateY(-50%);
    width: 600px;
    height: 600px;
    color: var(--text-faint);
    pointer-events: none;
    z-index: 0;
    animation: slowSpin 120s linear infinite;
  }
  @keyframes slowSpin {
    to { transform: translateY(-50%) rotate(360deg); }
  }

  .ftr__top {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(4, 1fr) 1.4fr;
    gap: 2.4rem;
    padding-bottom: var(--s-8);
  }

  .ftr__col { display: flex; flex-direction: column; gap: 1rem; }

  .ftr__col ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .ftr__col li {
    font-family: var(--font-cn);
    font-size: 0.84rem;
  }
  .ftr__col a {
    color: var(--text-soft);
    transition: color var(--dur-fast) var(--ease-soft);
  }
  .ftr__col a:hover { color: var(--text); }
  .ftr__col .muted { color: var(--text-muted); font-size: 0.8rem; }

  .ftr__brand { gap: 0.6rem; }
  .ftr__wordmark {
    font-family: var(--font-display);
    font-weight: 200;
    font-size: clamp(2.2rem, 4vw, 3rem);
    letter-spacing: -0.02em;
    color: var(--text);
    line-height: 1;
  }
  .ftr__tag {
    color: var(--text-dim);
    font-family: var(--font-cn);
    font-size: 0.85rem;
    margin: 0;
  }

  .ftr__live {
    display: inline-flex;
    align-items: center;
    gap: 0.85rem;
    margin-top: 0.6rem;
    padding: 0.45rem 0.9rem 0.45rem 0.6rem;
    background: rgba(255, 255, 255, 0.025);
    border: 1px solid var(--border);
    border-radius: var(--r-pill);
    width: fit-content;
  }
  .ftr__live-text {
    display: flex;
    flex-direction: column;
    line-height: 1.15;
    font-family: var(--font-mono);
  }
  .ftr__live-label {
    font-size: 0.62rem;
    letter-spacing: 0.32em;
    text-transform: uppercase;
    color: var(--text);
  }
  .ftr__live-since {
    font-size: 0.58rem;
    letter-spacing: 0.22em;
    color: var(--text-muted);
  }

  .ftr__bottom {
    border-top: 1px solid var(--border);
    padding-top: var(--s-5);
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: var(--text-muted);
  }
  .mono {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  @media (max-width: 1024px) {
    .ftr__top { grid-template-columns: repeat(2, 1fr); }
    .ftr__brand { grid-column: 1 / -1; order: -1; padding-bottom: 1rem; border-bottom: 1px solid var(--border); }
  }
  @media (max-width: 600px) {
    .ftr__top { grid-template-columns: 1fr 1fr; gap: 1.5rem; }
    .ftr__bottom { flex-direction: column; gap: 0.6rem; align-items: flex-start; }
    .ftr__site { font-size: 0.62rem; }
  }
</style>
